<% if @feed_items.any? %>
    <ol class="microposts">
        <%= render @feed_items %>
    </ol>

    <%== pagy_bootstrap_nav @pagy if @pagy %>
<% else %>
    <%= t("microposts.no_post") %>
<% end %>
